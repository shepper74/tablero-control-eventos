import React from 'react'
import ReactDOM from 'react-dom/client'
import Panel from './panel_seguridad_senado.JSX'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Panel />
  </React.StrictMode>,
)
